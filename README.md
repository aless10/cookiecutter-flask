## How to create the flask app from the template

1. create a python virtualenv and activate it
2. ``pip install cookiecutter`` or ``python -m pip install cookiecutter``
3. Run ``cookiecutter TemplateFlaskApp``
